{
  "name": "x3-linkedlist",
  "version": "0.1.0",
  "description": "",
  "engines": {
    "node": ">=8"
  },
  "main": "./dist/LinkedList.js",
  "scripts": {
    "test": "jest --coverage",
    "build": "rimraf dist/ && tsc",
    "watch": "rimraf dist/ && tsc -w",
    "doc": "rimraf docs/ && typedoc src/",
    "prepublishOnly": "npm run build && npm run test && npm run doc"
  },
  "author": "x3cion",
  "license": "MIT",
  "devDependencies": {
    "@types/jest": "24.0.15",
    "@types/node": "12.0.8",
    "jest": "24.8.0",
    "jest-extended": "0.11.1",
    "ts-jest": "24.0.2",
    "tslint": "5.17.0",
    "typedoc": "0.14.2",
    "typedoc-plugin-markdown": "2.0.6",
    "typescript": "3.5.2",
    "rimraf": "2.6.3"
  },
  "dependencies": {}
}